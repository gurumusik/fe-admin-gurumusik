version: '3.9'
services:
  vite-frontend:
    ports:
      - "5173:5173"
    build: .
    restart: always
    env_file:
      - .env
    networks:
      - localnet
    stdin_open: true
    tty: true

networks:
  localnet:
    driver: bridge
